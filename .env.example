# Public envs must be prefixed with NEXT_PUBLIC_
NEXT_PUBLIC_APP_NAME=GlowFactory
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000

# Server-only envs
NODE_ENV=development
# Add real secrets in your local .env (ignored by git)
